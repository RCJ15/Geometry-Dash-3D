using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace GD3D.UI
{
    /// <summary>
    /// A static class which contains data about the menu scenes (those being <see cref="MainMenu"/>, <see cref="IconKit"/> and <see cref="LevelSelect"/>).
    /// </summary>
    public static class MenuData
    {
        public static int LastActiveMenuSceneIndex;
    }
}
